# secrets
.env

# stray root-level copy of the dataset (canonical copy lives in data/)
/starter-datasets/
/starter-datasets.zip

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# collate runtime state
*.db
*.db-wal
*.db-shm
# ...except the built apparatus, which is committed so the UI and the demo run
# without an API key. Rebuild it with: python -m collate build <dir>
!collate.db
.cache/llm/
.cache/render/

# editors / os
.vscode/
.idea/
Thumbs.db
desktop.ini
